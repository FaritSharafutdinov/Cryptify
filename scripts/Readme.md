# 🤖 ML-Пайплайн: Сбор Данных, Обучение и Инференс

Этот документ описывает структуру, конфигурацию и порядок выполнения всех скриптов, относящихся к машинному обучению и работе с данными для MVP Criptify.

---

## 1. Структура Папки и Назначение Файлов

| Файл | Назначение |
| :--- | :--- |
| `docker-compose.yml` | Определяет сервисы **`db` (PostgreSQL)** и **`ml_test` (среда Python)**. |
| `init.sql` | Скрипт, который выполняется при старте DB и **создает таблицы** (`raw_bars`, `predictions`). |
| `requirements.txt` | Список зависимостей Python для ML-скриптов (`scikit-learn`, `sqlalchemy`, `pandas` и др.). |
| `data_fetcher.py` | Скрипт для извлечения 180 дней исторических данных BTCUSDT (1 час) с Bybit API и записи в DB. |
| `baseline_trainer.py` | Скрипт для **обучения Baseline-модели** (Линейная регрессия). |
| `inference.py` | Скрипт для **создания прогноза** и публикации его в DB. |
| **`baseline_model.joblib`** | **[АРТЕФАКТ]** Обученная модель (создается скриптом `baseline_trainer.py`). |

---

## 2. Инфраструктура и Контракт Базы Данных

Все ML-скрипты были запущены в контейнере `ml_test` и взаимодействовали с базой данных `db`.

### Настройка DB (Контракт для Backend)

Для подключения к базе данных из **любого другого контейнера** (например, Backend-сервиса) следует использовать следующие параметры, определенные в `docker-compose.yml`:

| Параметр | Значение | Примечание |
| :--- | :--- | :--- |
| **DB_HOST** | `db` | **Критично:** Имя сервиса PostgreSQL в Docker-сети (не `localhost`!) |
| **DB_PORT** | `5432` | |
| **DB_USER** | `user` | |
| **DB_PASSWORD** | `password` | |
| **DB_NAME** | `my_database` | |

### Запуск сервисов

```bash
# Запуск DB и ML-среды
docker-compose up -d
